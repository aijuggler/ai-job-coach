# LLM-Powered Job Interview Coach (Skeleton)

This is the **clean baseline structure** (no real logic yet).

## Root Files
- `main.py` : Streamlit entry
- `app.py`  : FastAPI entry
- `requirements.txt` : Dependencies
- `.env.example` : Environment variable template
- `pyproject.toml` (optional packaging info)
- `.gitignore`

## Next Milestone (M1)
Implement extraction logic inside `app/pipeline/extraction.py` and corresponding models in `app/models/`.

